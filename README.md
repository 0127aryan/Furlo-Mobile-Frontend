# Furlo-Mobile-Frontend

Expo (React Native) app for Furlo. TypeScript + `expo-router` (`src/app`).

## Run

```bash
npm install
npx expo start
```

Then press `a` to open on an Android emulator with Expo Go (Windows). iOS Simulator requires macOS.

Routes live under `src/app`. `@/` maps to `src/`.
