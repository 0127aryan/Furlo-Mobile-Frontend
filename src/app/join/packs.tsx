import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function JoinPacksScreen() {
  return (
    <ScreenPlaceholder title="Find your community" subtitle="Join packs during onboarding." />
  );
}
