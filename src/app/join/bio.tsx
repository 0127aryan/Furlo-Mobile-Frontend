import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function BioScreen() {
  return <ScreenPlaceholder title="Bio" subtitle="Optional bio and personality tags." />;
}
