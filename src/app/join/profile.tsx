import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function PetProfileScreen() {
  return (
    <ScreenPlaceholder title="Pet profile" subtitle="Name, pack name, breed, city, and photo." />
  );
}
