import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function SelectRoleScreen() {
  return (
    <ScreenPlaceholder title="Who are you?" subtitle="Pet parent or I love pets — onboarding step 1." />
  );
}
