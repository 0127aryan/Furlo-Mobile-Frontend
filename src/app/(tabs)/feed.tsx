import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function FeedScreen() {
  return <ScreenPlaceholder title="The Yard" subtitle="Feed — create, like, comment, and report come next." />;
}
