import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function PacksScreen() {
  return (
    <ScreenPlaceholder
      title="Packs"
      subtitle="Community discover is a stub on web too. Onboarding packs live at /join/packs."
    />
  );
}
