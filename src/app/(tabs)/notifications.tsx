import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function NotificationsScreen() {
  return (
    <ScreenPlaceholder title="Notifications" subtitle="No notifications API yet — coming soon." />
  );
}
